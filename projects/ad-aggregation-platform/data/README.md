# Data Directory Policy

This repository no longer tracks large House Ads datasets.

Use Supabase as the default runtime source (`HOUSE_ADS_SOURCE=supabase`).
Only keep minimal placeholders here for local script path compatibility.
